import React from 'react'

const Header = props => {
    return (
        <header className="text-center" style={{backgroundColor: ''}}>
            <h1><u>Login</u></h1>
        </header>
    )
}

export default Header